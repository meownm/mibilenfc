buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.3.2"
    }
}

/* Groovy way */
task clean(type: Delete) {
    delete rootProject.buildDir
}
