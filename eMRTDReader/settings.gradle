pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

// dependencyResolutionManagement is removed to allow project-level repositories

rootProject.name = "eMRTDReader"
include(":app")
